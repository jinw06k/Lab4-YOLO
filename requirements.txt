opencv-python==4.11.0.86
numpy
# onnxruntime is REQUIRED for yolo26 (--backend ort); OpenCV DNN can't run its
# NMS-free head. Not needed for yolov8n/yolo11n. Its aarch64 wheel DOES run on
# the Pi Zero 2W's Cortex-A53 (unlike torch — verified 2026-06-09).
onnxruntime
