# YOLO via the Ultralytics library (the standard YOLO API). Pulls in PyTorch.
# Pin to the version the lab was tested with so future runs are reproducible:
#   pip show ultralytics   # then set the version below
ultralytics
opencv-python   # camera capture, JPEG encoding for the stream, drawing boxes
numpy
